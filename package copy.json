{
  "name": "hospital-management-system",
  "version": "1.0.0",
  "description": "Professional Hospital Management System with Offline Support",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "install-all": "npm install && npm install --workspace=client && npm install --workspace=server",
    "dev": "npm run dev --workspace=server & sleep 2 && npm run dev --workspace=client",
    "build": "npm run build --workspace=client && npm run build --workspace=server"
  }
}
